# docker-compose.yml が参照する環境変数。
# コピーして .env として使う: cp .env.example .env
#
# 写真ライブラリのホスト側パス。コンテナには /photos として読み取り専用でマウントされる。
# UIの「フォルダを追加」にはコンテナ内パス (/photos/... 以下) を登録すること
PHOTO_LIBRARY_PATH=/path/to/your/photos
